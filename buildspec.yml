version: 0.2

phases:
  build:
    commands:
      - echo "Starting build..."
      - echo "Checking website files..."
      - test -f index.html
      - test -f appspec.yml
      - echo "Build completed successfully!"

artifacts:
  files:
    - index.html
    - appspec.yml
    - scripts/**/*
